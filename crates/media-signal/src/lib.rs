//! WebRTC signal message types
//!
//! Scaffold only — implementation follows the PR plan in
//! `docs/architecture/canislink-system-architecture.md`.

/// Crate is intentionally empty until its PR lands.
pub fn scaffold_ok() -> bool {
    true
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn scaffold() {
        assert!(scaffold_ok());
    }
}
