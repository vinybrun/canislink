# Feature 05 — Policy + steward

- Sleep window, e-stop, social_disabled, invite rate limit
- device-api 1s policy tick ends max-duration / segment / estop sessions
- Steward routes: `/v1/steward/estop`, `social_disabled`, `bonds`, `policy`
